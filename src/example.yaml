env: dev

storage:
  pdo:
    dns: $env(DB_PDO_DNS)
    username: $env(DB_PDO_USERNAME)
    password: $env(DB_PDO_PASSWORD)
  migration-directory: tests/Migrations
  entity-directory: tests/MockUps

php-formatter:
  validator:
    path-to-php: $env(PATH_TO_PHP)
